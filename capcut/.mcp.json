{
  "mcpServers": {
    "capcut": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/server.mjs"],
      "env": {
        "NODE_PATH": "${CLAUDE_PLUGIN_ROOT}/server/node_modules"
      }
    }
  }
}
